\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{mathbook}[2013/03/14 macros for preparing math books]

% Dla równań, amsldoc.pdf
%equation-numerowane, equation*- nie numerowane

%%%Str65 Rozdział 3 Równania liniowe, macierze, wyznaczniki
%\chapter{Równania liniowe, macierze, wyznaczniki}{3}
%W rozdziale  \ref{ssec:3} pokazano co następuje:

\let\chapterLaTeX\chapter
\renewcommand{\chapter}[2]{\chapterLaTeX{#1}\label{ssec:#2}}

%%%Str65 15. Funkcja liniowa. Zależnośc liniowa  
%\section{Funkcja liniowa. Zależnośc liniowa}{15}

%strona 115 Odwołanie przez \ref{sec:15}

\let\sectionLaTeX\section
\renewcommand{\section}[2]{\sectionLaTeX{#1}\label{sec:#2}}


%%%Strona 65 Funkcja liniowa jednej zmiennej.
%\subtytul{Funkcja liniowa jednej zmiennej}
%Funkcja $y$ określona w zbiorze $R$ liczb rzeczywistych okręlonych wzorem
%\begin{equation}
%y(x)=a\,x +b \qquad (a,\, b \text{ --- stałe})
%\label{eq:15.1}
%\end{equation}

\newcommand{\subtytul}[1]{\paragraph{#1}}


%str30 t2 Rozkład dowolnego wielomianu
%\subtytull{Rozkład dowolnego wielomianu na czynniki (rzeczywiste)}
\newcommand{\subtytull}[1]{\subsection*{#1}}


%%%str. 65  15.1. Jeżeli y jest funkcją liniową
%Uwaga podstawową czcionką jest pochylona, więc chcąc uzyskać zwykłą czcionkę w tym fragmencie należy użyć \emph{} np. str 66
%tw 15.7

%\begin{pkt}{15.1}
%Jeżeli $y$ jest funkcją liniową $x$, to przyrost tej funkcji jest proporcjonalny do przyrostu zmiennej  ...
%\end{pkt}

\newcounter{pkt}[section]
\renewcommand{\thepkt}{\arabic{section}.\arabic{pkt}}
\newenvironment{pkt}[1]
{
 \vskip\baselineskip\noindent%
 \refstepcounter{pkt}\label{pkt:#1}%
 \textbf{\thepkt.} \begin{small}\itshape
}
{\end{small}}

%%%%Dowód czcionką normalną.
%\begin{dowod}

%\end{dowod}
%t1 48
\newenvironment{dowod}
{
 \vskip\baselineskip\noindent%
 \begingroup\normalfont\textbf{Dowód.}
}
{\fbox{ }\endgroup}

\newenvironment{umowa}
{
 \vskip\baselineskip\noindent%
 \begingroup\normalfont\textbf{Umowa.}
}
{\fbox{ }\endgroup}

\newcounter{przyklad}[section]
\renewcommand{\theprzyklad}{\arabic{section}.\arabic{przyklad}}
\newenvironment{przyklad}[1]
{
 \vskip\baselineskip\noindent%
 \refstepcounter{przyklad}\label{ex:#1}%
 \textbf{Przykład \theprzyklad.} \begin{small}
}
{\end{small}}

\newenvironment{uwaga}[1]
{
 \vskip\baselineskip\noindent%
 \begingroup\normalfont\textbf{Uwaga #1.}
}
{\fbox{ }\endgroup}

\newenvironment{odcinek}
{
 \vskip\baselineskip\noindent%
 \begingroup\normalfont\textbf{Odcinek.}
}
{\fbox{ }\endgroup}

\newenvironment{przypadki}
{
 \vskip\baselineskip\noindent%
 \begingroup\normalfont\textbf{Przypadki szczególne.}
}
{\fbox{ }\endgroup}
%%%Strona 71. 15.1. Poniższe dwa przykłady, w których
%\begin{przyklad}{15.1}
%Kombinacją liniową funkcji
%\begin{equarion*}......
%... otrzymujemy \eqref{15.19}.
%\end{przyklad}

%W przykładzie~\ref{ex:15.1}


%Strona 75 Uwagi ogólne o układach równań.
%\subtytul{Uwagi ogólne o układach równań}
%Załóżmy, że funkcja $f_1,\ldots,\f_m,\, m\geq 1$, argumentu $x=(x_1,\ldots, x_n)$, mają wspólną dziedzinę $D$, zawartą w $\Re^2$ i rozważmy układ równań
%\begin{equation}
%f_1(x)=0,\ldots,f_m(x)=0\qquad m\geq 1
%\label{eq:16.3}
%\end{equation}


%Mówimy, że układ $F$ \emph{implikuje} układ $G$ (albo: $G$ \emph{wynika} z układu $F$)
%\begin{equation*}
%F\Rightarrow G
%\end{equation*}




%Uczyń spójniki niełamliwymi
\endinput





